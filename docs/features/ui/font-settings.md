# 字体设置

字体设置用于替换或调整终端字体。

## 入口

```text
美化/UI 功能 > 字体设置
```

## 字体目录

ZeroTermux 会使用内部存储下的字体目录：

```text
/sdcard/xinhao/font/
```

可以把字体文件放入该目录后，在字体设置中选择。

## 建议

- 优先使用等宽字体。
- 如果中文显示异常，换用包含中文字符的字体。
- 修改字体后如未立即生效，重启当前会话或重启应用。
