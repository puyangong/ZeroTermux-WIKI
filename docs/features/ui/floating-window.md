# 悬浮窗口

悬浮窗口用于把 ZeroTermux 的终端或工具以浮窗方式显示，便于在其他应用上方操作。

## 入口

```text
美化/UI 功能 > 悬浮窗口
```

## 使用前准备

1. 在系统设置中允许 ZeroTermux 显示在其他应用上层。
2. 确认当前终端会话可以正常运行。
3. 如果系统有后台限制，允许 ZeroTermux 后台运行。

## 建议

- 浮窗适合临时查看日志或执行短命令。
- 长时间运行任务仍建议回到主界面，避免系统回收浮窗进程。
