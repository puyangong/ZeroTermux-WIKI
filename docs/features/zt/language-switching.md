# 语言切换

ZeroTermux 支持中文和英文相关资源。语言切换入口用于调整应用显示语言。

## 入口

```text
ZT 功能 > 语言切换
```

## 使用说明

1. 打开语言切换。
2. 选择需要的语言。
3. 如界面没有立即刷新，重启当前 Activity 或重启应用。

## 注意事项

- 部分 Termux 原生文本由底层组件提供，可能不会随 ZeroTermux 语言设置变化。
- 脚本输出语言取决于脚本本身和系统环境。
